'use strict';

describe('transport', function () {
  this.timeout();

  it('can be described using', function () {
  });
});

benchmark('name of the benchmark', function (exec) {
  this.against('1.4.0');
});

memory('test leak', function () {
  this.tolerance('450kb');
});

get('/foo', function incoming(req, res) {

});
